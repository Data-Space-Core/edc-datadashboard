apiVersion: v1
kind: Service
metadata:
  name: edc-datadashboard
  namespace: edc-datadashboard
spec:
  selector:
    app: edc-datadashboard
  ports:
    - name: http
      port: 80
      targetPort: 8080

